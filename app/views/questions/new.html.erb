<h1>New question</h1>

<%= form_for @question do |f| %>
<%= f.hidden_field :user_name, :value => "#{@user = User.find(session[:user_id]).email}" %>
<%= f.label :title %>
<%= f.text_field :title %>
<hr>
<%= f.label :body %>
<%= f.text_area :body %>

<%= f.submit  %>
<% end %>
